`11:01`
### 할 일
우선 쿼리의 테이블명, 컬럼명 등으로 인해 배포 시 SQL 에러를 일으켰던 서버의 쿼리문을 수정하려 합니다.

추가로 이전에 `.gitignore`에 `*.properties`를 추가해두었어서 해당 행들을 삭제하였습니다.

`12:49`
### 작업 내용
이전 DB의 사용자 계정은 `root`, 비밀번호는 `1234`, 스키마는 `kevin`을 사용했습니다.
이는 애플리케이션이 관리자 권한의 DB 계정으로 접속하게 되는 문제가 있었습니다.
따라서 권한과 스키마 구조를 변경하였습니다.
[자세한 구조](../../ornably/docker/sql_structure.md)

DB의 테이블명은 대문자였지만 일부 쿼리([CartRepository의 SELECT_ALL_CART](https://github.com/hurwan0629/ornably-hurwan-refactoring/blob/main/ornably/src/main/java/bugsandwich/ornably/cart/CartRepository.java))에서는 소문자 테이블명을 사용했었습니다.
이 때문에 Linux 환경의 MySQL에서 대소문자 구분 문제에 의해 SQL 에러가 발생했습니다.

우선 사용자/스키마 문제는 `docker-compose*`와 `application*.properties`를 고치며 해결하였고, 모든 Repo와 Mapper, `*.sql`을 순회하며 쿼리를 수정하였습니다.
또한 `/ornably/docker/sql_structure.md`, `execute.md`를 통해 프로젝트 구조를 보여주는 문서를 작성하였습니다.

### 후기
검증과 수정범위 확인에 2시간이 걸렸습니다. 물론 제가 자세히 몰랐던 내용들을 공부하면서 일어났던 문제도 있고, 익숙하지 않았기에 방향성을 잡기 위해 고민하는 비용이 크긴 했었지만 작업량이 예상치보다 3할정도 더 있었습니다.

작업의 AI 요약은 [여기](../../ornably/ai-docs/2026-05-15_13_05.md)에 있습니다.
구체적인 쿼리 변경사항은 Git commit 기록을 통해 확인할 수 있습니다.

이번 작업을 통해 DB의 권한 구조를 더 자세히 알 수 있었으며 작업 도중 Docker Compose의 빌드 과정을 되짚어 보며 흐름을 더 명확히 복습할 수 있었습니다.

`17:50`
### 작업 내용
백엔드 서버 로직의 cors 설정중 프론트엔드 출처가 `http://localhost:5173` 로 고정되어있었던 것을 환경변수로 관리가 가능한 형태로 바꾸었습니다.
또한 샘플 데이터를 넣는 과정에서 도커를 통한 mysql의 `charset` 설정이 일부 `latin1`로 되는 문제로 인해 샘플데이터를 넣을 때 한글이 깨지던 현상을 `SET NAMES utf8mb4`를 통해 해결하였습니다.

프론트에서는 포트원과 API_URL에 대한 환경변수를 입력할 .env의 형태를 나타내는 .env.example를 추가하였으며 백엔드 서버로 요청하는 `axios`객체를 재사용 가능한 형태로 `lib/`에 만들어 열할을 분리하였습니다.